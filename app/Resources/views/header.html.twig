<header>
    <nav class="navbar" role="navigation" aria-label="main navigation">
        <div class="navbar-brand">
            <a role="button" class="navbar-burger" data-target="navbar" aria-label="menu" aria-expanded="false">
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
                <span aria-hidden="true"></span>
            </a>
        </div>

        <div id="navbar" class="navbar-menu">
            <div class="navbar-start">
                <a class="navbar-item" href="{{ path('homepage') }}">Accueil</a> 
                {% if app.session.get("isConnected") %}
                    <a class="navbar-item" href="{{ path('pokemon_index') }}">Pokemon</a>
                    <div class="navbar-item has-dropdown is-hoverable">
                        <a class="navbar-link" href="{{ path('refpokemon_index') }}">Pokedex</a>
                        <div class="navbar-dropdown">
                            <a class="navbar-item" href="{{ path('refpokemon_index') }}">Consulter le Pokedex</a>
                            <a class="navbar-item" href="{{ path('refpokemon_new') }}">Ajouter un nouveau Pokemon</a>
                            <a class="navbar-item" href="{{ path('refpokemon_stats') }}">Nombre de Pokemons par type</a>
                        </div>
                    </div>
                {% endif %}
            </div>
            <div class="navbar-end">
                <div class="navbar-item">
                    {% if app.session.get("isConnected") %}
                        <div class="navbar-item"><b>{{ app.session.get("trainer").username }}</b></div>
                        <div class="navbar-item">
                            <div>{{ app.session.get("trainer").nbPieces }}</div>
                            <span class="icon is-small is-left"><i class="fas fa-coins"></i></span>
                        </div>
                        <div><a class="button is-light" href="{{ path('disconnect') }}">Déconnexion</a></div>
                    {% else %}
                        <div class="buttons">
                            <a class="button is-primary" href="{{ path('inscription') }}"><strong>Inscription</strong></a>
                            <a class="button is-light" href="{{ path('login') }}">Connexion</a>
                        </div>
                    {% endif %}
                </div>
            </div>
        </div>
    </nav>
</header>